android {
            defaultConfig {
                applicationId = "br.com.test"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }